{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"liulab-newpkg","text":"<p>This is the Liu Lab repo template with its placeholder package still in place. The package is called <code>liulab-newpkg</code> and imports as <code>newpkg</code>. What the template ships, and how to make a repo from it, is on The template.</p> <p>One line: what this repo is for. <code>init-repo</code> rewrites this page.</p>","path":["liulab-newpkg"],"tags":[]},{"location":"#install-it","level":2,"title":"Install it","text":"<p>The repo uses pixi and nothing else. No pip, no conda, no uv. Clone the repo, then:</p> <pre><code>pixi install\n</code></pre> <p>That reads <code>pyproject.toml</code> and builds the environment from the lock file, so you get the same versions the tests ran on.</p>","path":["liulab-newpkg"],"tags":[]},{"location":"#use-it","level":2,"title":"Use it","text":"<pre><code>from newpkg import greet\n\nprint(greet(\"lab\"))\n</code></pre> <p>The same thing from a shell:</p> <pre><code>pixi run newpkg greet lab\n</code></pre> <p>The API reference has the full list, built from the docstrings in <code>src/</code>.</p>","path":["liulab-newpkg"],"tags":[]},{"location":"#check-your-work","level":2,"title":"Check your work","text":"<p>One command runs the linters, the type checker and the tests:</p> <pre><code>pixi run check\n</code></pre> <p>It runs every step, then prints all the failures at once. Read to the bottom before you fix anything.</p> <p>The docs site is built by a separate command, because it needs a heavier environment:</p> <pre><code>pixi run docs-build\n</code></pre>","path":["liulab-newpkg"],"tags":[]},{"location":"#where-things-live","level":2,"title":"Where things live","text":"Path What it holds <code>src/newpkg/</code> the package <code>tests/</code> the tests <code>docs/</code> this site <code>scripts/check.sh</code> the gate every commit has to pass <code>CONTEXT.md</code> the glossary: the words this repo uses <p>Some notes are written for coding agents, not for people. Conventions go under <code>docs/agents/</code>, decision records under <code>docs/adr/</code>, and research notes under <code>docs/research/</code>. Nothing in those three directories shows up in the menu or the search box, and a page written there is still reachable by its own URL.</p>","path":["liulab-newpkg"],"tags":[]},{"location":"api/","level":1,"title":"API reference","text":"<p>Built from the docstrings in <code>src/newpkg/</code>, so this page and the code cannot drift apart. Write the docstring; this page follows.</p>","path":["API reference"],"tags":[]},{"location":"api/#the-examples-are-tests","level":2,"title":"The examples are tests","text":"<p><code>pixi run check</code> runs the <code>Examples</code> blocks in <code>src/newpkg/</code>, so an example that no longer matches its code fails the tests.</p> <p>Write one where it makes the object easier to use, and leave it out where it would not. An example nobody keeps up to date is worse than none.</p> <p>Keep an example cheap, offline and deterministic. It has to give the same answer on any machine, with no network. A line that cannot do that needs <code># doctest: +SKIP</code> at the end of that line.</p> <p>Two things about that marker are easy to get backwards:</p> <ul> <li>It covers only the line it sits on. It does not carry to the line below. In a block that   mixes lines that run with lines that cannot, each line that cannot run needs its own.</li> <li>A trailing comment written as plain prose looks just like a marker and is not one. Only   the <code># doctest:</code> form is read as one.</li> </ul>","path":["API reference"],"tags":[]},{"location":"api/#newpkg","level":2,"title":"newpkg","text":"","path":["API reference"],"tags":[]},{"location":"api/#newpkg.core.greet","level":2,"title":"newpkg.core.greet","text":"<pre><code>greet(name: str = 'world') -&gt; str\n</code></pre> <p>Return a greeting addressed to <code>name</code>.</p> <p>The numpydoc section form is demonstrated once here, by <code>Examples</code>; a function this small would not normally carry a section at all.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; greet(\"lab\")\n'Hello, lab!'\n</code></pre> Source code in <code>src/newpkg/core.py</code> <pre><code>def greet(name: str = \"world\") -&gt; str:\n    \"\"\"Return a greeting addressed to `name`.\n\n    The numpydoc section form is demonstrated once here, by `Examples`; a function this small\n    would not normally carry a section at all.\n\n    Examples\n    --------\n    &gt;&gt;&gt; greet(\"lab\")\n    'Hello, lab!'\n    \"\"\"\n    return f\"Hello, {name}!\"\n</code></pre>","path":["API reference"],"tags":[]},{"location":"api/#the-command-line","level":2,"title":"The command line","text":"<p>The whole module, because typer makes every verb a plain function with a docstring, and <code>newpkg.cli:app</code> — the object <code>[project.scripts]</code> registers — is built from them.</p>","path":["API reference"],"tags":[]},{"location":"api/#newpkg.cli","level":2,"title":"newpkg.cli","text":"<p>The command line. One verb and a version, so <code>[project.scripts]</code> has something to register.</p> <p>Typer, because every lab repo that ships a command line uses it: one <code>typer.Typer</code> named <code>app</code>, <code>no_args_is_help=True</code> so a bare invocation prints help instead of nothing, and a <code>version</code> command. A larger surface grows by mounting sub-apps with <code>app.add_typer</code>.</p>","path":["API reference"],"tags":[]},{"location":"api/#newpkg.cli.greet","level":3,"title":"greet","text":"<pre><code>greet(\n    name: Annotated[\n        str, Argument(help=\"Who to greet.\")\n    ] = \"world\",\n) -&gt; None\n</code></pre> <p>Print a greeting addressed to NAME.</p> Source code in <code>src/newpkg/cli.py</code> <pre><code>@app.command()\ndef greet(name: Annotated[str, typer.Argument(help=\"Who to greet.\")] = \"world\") -&gt; None:\n    \"\"\"Print a greeting addressed to NAME.\"\"\"\n    typer.echo(_greet(name))\n</code></pre>","path":["API reference"],"tags":[]},{"location":"api/#newpkg.cli.version","level":3,"title":"version","text":"<pre><code>version() -&gt; None\n</code></pre> <p>Print the installed package version.</p> Source code in <code>src/newpkg/cli.py</code> <pre><code>@app.command()\ndef version() -&gt; None:\n    \"\"\"Print the installed package version.\"\"\"\n    typer.echo(_package_version)\n</code></pre>","path":["API reference"],"tags":[]},{"location":"template/","level":1,"title":"The Liu Lab repo template","text":"<p>This repo is where a new Liu Lab repo starts. It is a complete, working repo wearing a placeholder name: it builds, it tests, it publishes this site, and every pull request on it passes the same rules it hands to the repos made from it. That is the point. The template is evidence, not a promise.</p> <p>The demo repo was made from this template and left exactly as <code>init-repo</code> finished it. Read it beside this one to see what changes and what stays.</p>","path":["The Liu Lab repo template"],"tags":[]},{"location":"template/#what-it-ships","level":2,"title":"What it ships","text":"What Detail One toolchain pixi, and nothing else. No pip, no conda, no uv. <code>pyproject.toml</code> is the one place dependencies, environments and tasks are declared. A working package A small Python package with a command, tests that pass, and an API page built from its docstrings. One gate <code>pixi run check</code> runs ruff, pyright, vale, markdownlint, a conformance check and pytest. It runs every step and prints all the failures at once. Writing rules Vale checks them: word caps on the notes written for agents, plain language on the pages written for people. A docs site This one. Built by zensical, published to GitHub Pages by a workflow. Skills for agents Tracked links in <code>.claude/skills/</code> and <code>.agents/skills/</code> point at <code>skills/</code>, so an agent finds them the moment you clone. You install nothing. Notes for agents <code>AGENTS.md</code>, <code>CONTEXT.md</code> and <code>docs/agents/</code> are written for a machine that has to act. They stay out of this menu and out of the search box.","path":["The Liu Lab repo template"],"tags":[]},{"location":"template/#making-a-repo-from-it","level":2,"title":"Making a repo from it","text":"<ol> <li>Press Use this template on GitHub, then Create a new repository.</li> <li>Clone the new repo.</li> <li>Open a coding agent in it and say you just made a repo from the template.</li> </ol> <p>The <code>init-repo</code> skill takes it from there. It asks four things: the shape of the repo, the module name, whether you want a command, and one line on what the repo is for. The shape is a ladder — a package the lab publishes, a package it keeps to itself, or no package at all.</p> <p>A script then does every mechanical step. It renames the placeholder package, drops the parts you said you do not need, rewrites the README, commits, and tags a first version. If you would rather skip the agent, run it yourself:</p> <pre><code>python scripts/init_repo.py --help\n</code></pre> <p>It is careful about your work in a way worth knowing. Renaming always runs. A deletion or an overwrite runs unasked only while the file is still byte-identical to the first commit, which is what a repo made from the button has. Anything you already edited, it asks about, and the default answer is keep. <code>--plan</code> shows you the whole survey and changes nothing.</p>","path":["The Liu Lab repo template"],"tags":[]},{"location":"template/#what-init-repo-subtracts","level":2,"title":"What init-repo subtracts","text":"<p>A new repo should not inherit the machinery that made it. So the script deletes:</p> <ul> <li>both agent skills — <code>init-repo</code> and <code>template-dev</code> — and their tracked links</li> <li><code>scripts/init_repo.py</code>, <code>scripts/dogfood.py</code>, and the CI job that renders this template</li> <li>this page, and the config file that names the site after the template</li> <li>the template's own decision record and research notes, plus every sentence in a surviving   file that pointed at one of them</li> <li>the entries in <code>CHANGELOG.md</code>, which are this template's history and not the new repo's.   The file itself always ships.</li> </ul> <p>Then the shape decides the rest. A repo that does not publish loses the release workflow. A repo with no package also loses <code>src/</code> and <code>tests/</code>.</p> <p>Nothing above is a claim you have to take on faith. Every pull request here renders the template three times, once per shape, and hands each result to its own gate and its own docs build. A file that should have gone, or a link left pointing at one, fails the build in this repo rather than in yours.</p>","path":["The Liu Lab repo template"],"tags":[]}]}